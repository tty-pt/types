#define VSS_DIV
#include "vss/vss.h"
FLEX
CALL(ALL_AXIS, BASE_SIZES)
PADDING()
FONT_WEIGHT(bold)
MARGIN_DIR(left, 8)
COLOR(invalid)
OUTLINE(border)
TABLE_LAYOUT(fixed)
FULL_SIZE_horizontal
OVERFLOW(hidden)
TEXT_OVERFLOW(ellipsis)
mark.mark {
	color: inherit;
	font-weight: bold;
	background-color: inherit;
}
.tnow { white-space: nowrap; }
